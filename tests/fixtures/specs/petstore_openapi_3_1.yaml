openapi: "3.1.0"
info:
  title: Petstore
  version: "1.0.0"
servers:
  - url: https://petstore.example.com/api/v1
paths:
  /pets:
    get:
      operationId: listPets
      responses:
        "200":
          description: List of pets
